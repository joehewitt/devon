/**
 * This is a precompiled header.  Common used headers are included here to optimize compile times.
 * Do not, however, depend on the header files here being always included.  That is what
 * global.h is for.  The project should still compile even if pch.h were removed.
 */

// STL
#include <exception>
#include <string>
#include <vector>
#include <map>
#include <iostream>
#include <sstream>
#include <algorithm>
